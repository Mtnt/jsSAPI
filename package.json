{
  "name": "jssapi",
  "version": "0.1.0",
  "description": "JS Storage API that provides you namespaceses, validation and shortcuts.",
  "main": "dest/index.js",
  "scripts": {
    "build": "babel src --out-dir dist",
    "test": "jest",
    "fast-clean-install": "rm -rf ./node_modules && npm install",
    "prepublishOnly": "npm run build",
    "postpublish": "rm -rf dist"
  },
  "keywords": [
    "namespace",
    "storage",
    "interface"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "lodash": "^4.17.4",
    "es6-error": "^4.0.2"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-jest": "^21.0.2",
    "babel-plugin-transform-class-properties": "^6.24.1",
    "babel-preset-env": "^1.6.0",
    "jest": "^21.1.0"
  }
}
